[package]
name = "private_airdrop_core"
version = "0.1.0"
edition = "2021"

[dependencies]
nssa_core = { path = "../../../nssa/core" }
borsh = { workspace = true }
serde = { workspace = true, features = ["derive"] }
sha2 = "0.10"

[lib]
path = "src/lib.rs"
